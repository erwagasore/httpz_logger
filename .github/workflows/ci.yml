name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Enable when httpz dependency supports Zig 0.15+
      # Currently httpz uses deprecated linkLibC() API incompatible with Zig master,
      # but this library uses std.Io.Writer which requires Zig 0.15+
      - name: CI temporarily disabled
        run: echo "CI disabled - waiting for httpz to support Zig 0.15+"
